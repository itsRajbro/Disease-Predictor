services:
  - type: web
    name: mediscan-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    disk:
      name: models-disk
      mountPath: /models
      sizeGB: 2